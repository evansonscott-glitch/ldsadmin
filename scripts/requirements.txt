# Sam LDS Assistant - Python Dependencies
# Install: pip install -r requirements.txt

# Browser automation for LCR
playwright>=1.40.0

# Gmail IMAP/SMTP (standard library, but noting version)
# imaplib, smtplib, email - included in Python 3.8+

# Google APIs for Calendar/Drive
google-api-python-client>=2.100.0
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1

# Utilities
python-dateutil>=2.8.2
